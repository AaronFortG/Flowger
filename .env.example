# ── Required ──────────────────────────────────────────────────────────────────
ENABLEBANKING_APP_ID=your_app_id_here
ENABLEBANKING_KEY_PATH=/path/to/private.key

# ── Required (unless specified via --bank and --country options to allow multiple banks) ─────────────
# Set these if you want to run commands without passing --bank and --country every time.
# DEFAULT_BANK=
# DEFAULT_COUNTRY=

# ── Optional ──────────────────────────────────────────────────────────────────
# DATABASE_PATH=flowger.db
# DEFAULT_REDIRECT_URL=https://enablebanking.com/ais/
# DEFAULT_EXPORT_FILE=transactions.csv
